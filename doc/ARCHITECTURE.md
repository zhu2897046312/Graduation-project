# 项目架构文档

## 📋 项目概述

这是一个基于 **Nuxt 4** 构建的现代化电商前端项目，采用 Vue 3 + TypeScript 技术栈，实现了完整的电商购物流程，包括商品展示、购物车、订单管理、用户认证、支付等功能。

## 🛠 技术栈

### 核心框架
- **Nuxt 4.1.0** - Vue 3 全栈框架，支持 SSR/SSG
- **Vue 3.5.20** - 渐进式 JavaScript 框架
- **TypeScript 5.9.2** - 类型安全的 JavaScript 超集
- **Vue Router 4.5.1** - 官方路由管理器

### UI 框架
- **Naive UI 2.42.0** - Vue 3 组件库
- **@nuxt/ui 3.3.3** - Nuxt UI 组件
- **Tailwind CSS 3.4.10** - 实用优先的 CSS 框架
- **@css-render/vue3-ssr** - SSR 样式渲染支持

### 工具库
- **uuid 12.0.0** - 唯一标识符生成

## 📁 项目结构

```
shop_pc/
├── api/                    # API 接口层
│   ├── index.ts           # API 方法定义
│   └── type.ts            # TypeScript 类型定义
├── app/                    # 应用主目录
│   ├── app.vue            # 根组件
│   ├── assets/            # 静态资源
│   │   ├── css/          # 样式文件
│   │   └── images/       # 图片资源
│   ├── components/        # 可复用组件
│   │   ├── BaseNav.vue   # 基础导航组件
│   │   ├── ProductCard.vue # 商品卡片组件
│   │   ├── ProductGrllery.vue # 商品图片画廊
│   │   ├── ProductSkuBox.vue # SKU 选择组件
│   │   ├── LoginModal.vue # 登录模态框
│   │   ├── ShopNav.vue   # 商店导航
│   │   ├── UserAddressModal.vue # 地址选择模态框
│   │   └── UserAddressFormModal.vue # 地址表单模态框
│   ├── composable/        # 组合式函数
│   │   └── useSiteInfo.ts # 站点信息获取
│   ├── layouts/           # 布局组件
│   │   ├── default.vue   # 默认布局
│   │   ├── blank.vue     # 空白布局
│   │   └── minilayout.vue # 迷你布局
│   ├── pages/             # 页面路由（基于文件系统）
│   │   ├── index.vue     # 首页
│   │   ├── cart.vue      # 购物车页
│   │   ├── checkout.vue  # 结算页
│   │   ├── search.vue    # 搜索页
│   │   ├── account/      # 用户账户相关
│   │   │   ├── index.vue # 账户首页
│   │   │   ├── login.vue # 登录页
│   │   │   ├── register.vue # 注册页
│   │   │   ├── reset-pwd.vue # 重置密码页
│   │   │   └── addresses.vue # 地址管理页
│   │   ├── product/      # 商品相关
│   │   │   └── [id].vue  # 商品详情页
│   │   ├── collections/  # 分类集合
│   │   │   └── [code].vue # 分类页
│   │   ├── tag/          # 标签页
│   │   │   └── [code].vue # 标签商品列表
│   │   ├── blogs/        # 博客/文档
│   │   │   └── [code].vue # 文档详情页
│   │   ├── order-detail/ # 订单详情
│   │   │   └── [id].vue  # 订单详情页
│   │   └── reset-pwd/    # 密码重置
│   │       └── [code].vue # 重置密码（带验证码）
│   └── plugins/           # 插件
│       └── naive-ui.ts   # Naive UI SSR 支持
├── data/                  # 静态数据
│   └── address.json      # 地址数据
├── hook/                  # 自定义 Hooks
│   └── useCartHook.ts    # 购物车 Hook
├── server/                # 服务器端代码
│   ├── request.ts        # HTTP 请求封装
│   └── tsconfig.json     # TypeScript 配置
├── utils/                 # 工具函数
│   └── auth.ts           # 认证相关工具
├── public/                # 公共静态文件
│   ├── favicon.ico       # 网站图标
│   └── robots.txt        # 搜索引擎爬虫配置
├── nuxt.config.ts        # Nuxt 配置文件
├── package.json          # 项目依赖配置
└── tsconfig.json         # TypeScript 配置
```

## 🏗 架构设计

### 1. 分层架构

#### API 层 (`api/`)
负责统一管理所有后端 API 调用，采用模块化设计：

- **shop** - 电商核心功能模块
  - `category` - 分类管理
  - `product` - 商品管理
  - `user` - 用户认证
  - `cart` - 购物车
  - `address` - 地址管理
  - `order` - 订单管理
  - `market` - 市场信息（面包屑、站点信息、运费）
  - `tag` - 标签管理

- **blogs** - 内容管理模块
  - `document` - 文档管理
  - `recommend` - 推荐位管理

#### 服务器层 (`server/`)
- **HttpRequest 类** - 统一的 HTTP 请求封装
  - 自动添加认证 Token
  - 设备指纹识别（X-Device-Fingerprint）
  - 统一错误处理
  - 支持 GET/POST 请求

#### 页面层 (`app/pages/`)
基于 Nuxt 的文件系统路由，自动生成路由：
- 动态路由：`[id].vue`、`[code].vue`
- 嵌套路由：通过文件夹结构实现
- SEO 优化：支持服务端渲染

#### 组件层 (`app/components/`)
可复用的 Vue 组件：
- **BaseNav** - 基础导航栏（分类树）
- **ProductCard** - 商品卡片展示
- **ProductGrllery** - 商品图片画廊
- **ProductSkuBox** - SKU 选择器
- **LoginModal** - 登录模态框
- **UserAddressModal** - 地址选择器

#### 布局层 (`app/layouts/`)
- **default** - 默认布局（包含头部、导航、页脚）
- **blank** - 空白布局
- **minilayout** - 迷你布局

### 2. 数据流

```
用户操作
  ↓
页面组件 (Pages)
  ↓
组合式函数 (Composables) / Hooks
  ↓
API 层 (api/index.ts)
  ↓
HTTP 请求 (server/request.ts)
  ↓
后端 API
  ↓
响应数据
  ↓
状态管理 (useState)
  ↓
UI 更新
```

### 3. 状态管理

使用 Nuxt 的 `useState` 进行状态管理：
- `cartNum` - 购物车商品数量
- `showLoginModal` - 登录模态框显示状态
- `search` - 搜索关键词
- `price` - 商品价格（商品详情页）
- `current_sku_id` - 当前选中的 SKU ID

### 4. 认证机制

- **Token 认证**：使用 Cookie 存储 `accessToken`
- **设备指纹**：使用 UUID 生成设备唯一标识
- **自动登录检测**：通过 Token 获取当前用户信息

## 🔑 核心功能模块

### 1. 商品模块

#### 商品列表
- 支持分页、排序、筛选
- 热门商品展示
- 新品展示
- 标签筛选

#### 商品详情
- 商品图片画廊
- SKU 选择（支持多属性组合）
- 商品属性展示
- 商品详情/规格切换
- 相关商品推荐

#### 分类系统
- 多级分类树
- 分类面包屑导航
- 分类商品列表

### 2. 购物车模块

- 添加商品到购物车
- 修改商品数量
- 删除商品
- 实时计算总价
- 购物车数量徽标

### 3. 订单模块

- 订单创建
- 订单详情查看
- 订单列表
- 支付集成（PayPal）

### 4. 用户模块

- 用户注册
- 用户登录
- 密码重置（邮箱验证）
- 用户信息管理
- 地址管理（增删改查）

### 5. 搜索模块

- 关键词搜索
- 搜索结果展示

### 6. 内容模块

- 博客/文档展示
- 推荐位内容管理

## 🔧 配置说明

### Nuxt 配置 (`nuxt.config.ts`)

```typescript
{
  compatibilityDate: '2025-07-15',
  devtools: { enabled: true },
  
  // 构建配置
  build: {
    transpile: ['naive-ui', 'vueuc', '@css-render/vue3-ssr']
  },
  
  // CSS 配置
  css: ['~/assets/css/main.css'],
  
  // 模块
  modules: ['@nuxtjs/tailwindcss'],
  
  // 服务器配置
  nitro: {
    preset: 'node-server'
  },
  
  // 运行时配置
  runtimeConfig: {
    public: {
      apiUrl: process.env.NODE_ENV === 'production' 
        ? 'https://www.earring18.com/api/client'
        : 'http://localhost:8080/api/client'
    }
  },
  
  // 生产环境 ISR
  $production: {
    routeRules: {
      '/**': { isr: true }
    }
  }
}
```

### 环境配置

- **开发环境**：`http://localhost:8080/api/client`
- **生产环境**：`https://www.earring18.com/api/client`

## 📦 类型定义

项目使用 TypeScript 进行类型定义，主要类型包括：

- **Category** - 分类实体
- **ProductItem** - 商品项
- **ProductInfo** - 商品详情
- **CartItem** - 购物车项
- **Address** - 地址实体
- **DocumentItem** - 文档项
- **MarketInfo** - 市场信息
- **SpProductProdFrontVo** - 商品属性前端 VO
- **SpProductSkuFrontInfoVo** - SKU 前端信息 VO

详细类型定义见 `api/type.ts`

## 🎨 UI/UX 设计

### 设计风格
- 主色调：`#FB7F86`（粉红色）
- 辅助色：`#f4b3c2`（浅粉色）
- 文字色：`#878787`（灰色）
- 价格色：`#ff4d4f`（红色）

### 响应式设计
- 使用 Tailwind CSS 实现响应式布局
- 支持移动端和桌面端适配

### 交互特性
- 商品卡片悬停效果
- 购物车添加动画
- 模态框拖拽
- 加载状态提示

## 🔐 安全特性

1. **Token 认证**：所有需要认证的请求自动携带 Token
2. **设备指纹**：通过 UUID 生成设备唯一标识，用于风控
3. **XSS 防护**：使用 Vue 的模板语法自动转义
4. **CSRF 防护**：通过 Token 机制防护

## 🚀 部署说明

### 开发环境
```bash
npm run dev
```

### 生产构建
```bash
npm run build
```

### 预览生产构建
```bash
npm run preview
```

### 静态生成
```bash
npm run generate
```

## 📝 开发规范

### 代码组织
- 使用 TypeScript 进行类型约束
- 组件采用 Composition API
- 使用 `<script setup>` 语法
- 样式使用 Scoped CSS

### 命名规范
- 组件：PascalCase（如 `ProductCard.vue`）
- 文件：kebab-case（如 `use-site-info.ts`）
- 变量/函数：camelCase
- 常量：UPPER_SNAKE_CASE

### API 调用规范
- 统一通过 `api/index.ts` 调用
- 使用 `useAsyncData` 进行数据获取
- 错误统一处理

## 🔄 数据流示例

### 添加购物车流程

```
用户点击"添加到购物车"
  ↓
handleAddCart() 函数
  ↓
useCart().addCart()
  ↓
api.shop.cart.act()
  ↓
httpRequest.exec()
  ↓
后端 API
  ↓
更新购物车数量 (cartNum)
  ↓
显示成功提示
```

### 商品详情加载流程

```
访问 /product/[id]
  ↓
useAsyncData('product-${id}')
  ↓
api.shop.product.info(id)
  ↓
获取商品详情
  ↓
初始化 SKU、价格等状态
  ↓
渲染商品详情页
```

## 🐛 错误处理

- API 请求错误统一在 `server/request.ts` 中处理
- 使用 `Promise.reject` 抛出错误
- 页面级错误使用 `createError` 创建错误页面
- UI 错误提示使用 Naive UI 的 `useMessage` 和 `useDialog`

## 📚 依赖说明

### 核心依赖
- `nuxt` - Nuxt 框架
- `vue` - Vue 框架
- `vue-router` - 路由管理
- `typescript` - TypeScript 支持

### UI 依赖
- `naive-ui` - UI 组件库
- `@nuxt/ui` - Nuxt UI 组件
- `tailwindcss` - CSS 框架

### 工具依赖
- `uuid` - UUID 生成
- `@css-render/vue3-ssr` - SSR 样式渲染

## 🔮 未来优化方向

1. **性能优化**
   - 图片懒加载优化
   - 代码分割优化
   - 缓存策略优化

2. **功能扩展**
   - 商品收藏功能
   - 商品评价系统
   - 优惠券系统
   - 多语言支持

3. **用户体验**
   - PWA 支持
   - 离线访问
   - 推送通知

4. **技术升级**
   - 引入状态管理库（Pinia）
   - 单元测试覆盖
   - E2E 测试

## 📞 联系方式

项目维护者：请根据实际情况填写

---

**最后更新**：2025年1月
